import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  cardContainer: {
    maxWidth: '47%',
    display: 'inline-block',
    marginRight: '3%',
    marginBottom: '3%'
  }
});